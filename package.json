{
  "name": "SPS",
  "version": "1.0.0",
  "description": "A simple product store API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "build": "cd client && yarn install && yarn run build"
  },
  "engines": {
    "node": ">=14.16.0"
  },
  "author": "Ashraf Yusuf",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "express": "^4.17.1",
    "path": "^0.12.7"
  }
}
